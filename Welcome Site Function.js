//Building a function to greet user approriately based on time of day. Example: "Good morning" if logged on at 10AM.
let time;

let userLoggedInToday = True;
if (UserLoggedInToday && time === 12) {
    
    //insert action here
    console.log("Good afternoon, thanks for logging in today!");
    
}
else(){ //backup plan
  
  // insert action here
  
}
